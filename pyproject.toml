[project]
name = "codeql-miner"
version = "0.1.0"
description = "Miner automatizado de CodeQL para organizaciones de GitHub"
requires-python = ">=3.9"
dependencies = [
    "typer>=0.9.0",
    "pydantic>=2.0.0",
    "requests>=2.31.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0"
]
